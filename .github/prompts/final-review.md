# Final architecture review prompt (referenced by merge-gate.js via gate-config fields).
# Most FINAL review text is assembled dynamically from gate-config.js at runtime.

See `finalClaudeScope`, `finalClaudeProductValue` in `.github/scripts/gate-config.js`.
